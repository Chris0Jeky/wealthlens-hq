# Placeholder workflow spec.
#
# Move this file to .github/workflows/ when deployment is ready.

name: Deploy

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy workflow not implemented yet."
