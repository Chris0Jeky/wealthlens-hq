# The real deploy workflow lives at .github/workflows/deploy.yml.
# This file is kept as a pointer so the automation/ directory structure is clear.
#
# See .github/workflows/deploy.yml for the GitHub Pages deployment configuration.
