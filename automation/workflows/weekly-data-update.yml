# Placeholder workflow spec.
#
# Move this file to .github/workflows/ when the data pipeline is ready to run
# in CI. Keep it here while the automation design is still being drafted.

name: Weekly Data Update

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Weekly data update workflow not implemented yet."
