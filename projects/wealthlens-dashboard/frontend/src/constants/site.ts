/**
 * Canonical site identity — the single source for absolute URLs in meta
 * tags, canonical links, OG images, and the generated sitemap.
 *
 * This is the GitHub Pages URL on purpose: `wealthlens.uk` is not yet
 * registered (ACTION-REQUIRED #4). Until that decision, everything
 * standardises on this one identity (ADR 0001; RFC-001h) — change it in
 * exactly one place when the domain lands.
 */
export const SITE_URL = "https://chris0jeky.github.io/wealthlens-hq"

export const SITE_NAME = "WealthLens UK"
